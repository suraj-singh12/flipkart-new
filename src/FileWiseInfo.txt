=> index.js
React, ReactDOM from 'react-dom/client', 
bootstrap, Routing from '.routing.js'
root.render(<Routing />)



=> routing.js
React, {BrowserRouter, Route} from 'react-router-dom',
all components imported
Router = () => {
    return (
        <BrowserRouter>
            <Route exact path='/' component={Home} />
            <Route path="/listing:id" component={Listing} />
            ....
        </BrowserRouter>
    )
}
export default Router;



=> Home.js
